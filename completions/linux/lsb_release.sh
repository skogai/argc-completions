#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -v --version
# @flag -i --id
# @flag -d --description
# @flag -r --release
# @flag -c --codename
# @flag -a --all
# @flag -s --short
# @flag -h --help

command eval "$(argc --argc-eval "$0" "$@")"