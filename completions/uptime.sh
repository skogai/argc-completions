#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -c --container    show container uptime
# @flag -p --pretty       show uptime in pretty format
# @flag -r --raw          show uptime values in raw format
# @flag -s --since        system up since
# @flag -h --help         display this help and exit
# @flag -V --version      output version information and exit

command eval "$(argc --argc-eval "$0" "$@")"