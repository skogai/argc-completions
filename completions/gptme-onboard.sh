#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --check         Check setup status without making changes
# @flag -v --verbose    Enable verbose output
# @flag --help          Show this message and exit.

command eval "$(argc --argc-eval "$0" "$@")"