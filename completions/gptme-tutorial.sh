#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option --task <N>    Run a specific task (1-3) instead of all.
# @flag --help          Show this message and exit.

command eval "$(argc --argc-eval "$0" "$@")"