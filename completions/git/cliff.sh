#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

cliff() {
    :;
}

# {{{ git cliff config
# @cmd
cliff::config() {
    :;
}
# }}} git cliff config

# {{{ git cliff diff
# @cmd
cliff::diff() {
    :;
}
# }}} git cliff diff

command eval "$(argc --argc-eval "$0" "$@")"