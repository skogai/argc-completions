#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -4 --ipv4                    contact IPv4 hosts
# @flag -6 --ipv6                    contact IPv6 hosts
# @flag -A --active                  enable active mode transfer
# @flag -d --debug                   enable debugging output
# @flag -e --no-edit                 disable command line editing
# @flag -g --no-glob                 turn off file name globbing
# @flag -i --no-prompt               do not prompt during multiple file transfers
# @flag -n --no-login                do not automatically login to the remote system
# @option -N --netrc <netrc-file>    select a specific initialization file
# @option --prompt                   print a command line PROMPT (optionally), even if not on a tty
# @flag -p --passive                 enable passive mode transfer, default for ‘pftp'
# @flag -t --trace                   enable packet tracing
# @flag -v --verbose                 verbose output
# @flag -? --help                    give this help list
# @flag --usage                      give a short usage message
# @flag -V --version                 print program version
# @arg host

command eval "$(argc --argc-eval "$0" "$@")"