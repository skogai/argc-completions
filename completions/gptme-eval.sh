#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option -m --model <TEXT>                   Model to use, can be passed multiple times.
# @flag -t --timeout                          INTEGER RANGE  Timeout for code generation (seconds) [x>=1]
# @flag -p --parallel                         INTEGER RANGE  Number of parallel evals to run  [x>=1]
# @option --tool-format[markdown|xml|tool]    Tool format to use.
# @flag -l --list                             List available eval suites and tests, then exit.
# @flag --use-docker                          Run evals in Docker container for isolation (prevents host environment pollution)
# @flag --user-context                        Include user-level prompt files and agent instructions from ~/.config/gptme.
# @flag --no-user-context                     Include user-level prompt files and agent instructions from ~/.config/gptme.
# @flag --json                                Output results as JSON to stdout (also saves eval_results.json alongside CSV).
# @option -E --eval-module <FILE>             Load eval specs from an external Python module file (e.g. generated by speckit-eval gen).
# @flag --leaderboard                         Generate a model comparison leaderboard from eval_results/ instead of running evals.
# @option --leaderboard-format[rst|csv|markdown|json|html]  Output format for the leaderboard (default: markdown).
# @option --min-tests <INTEGER>               Minimum number of tests for a model to appear in the leaderboard (default: 4).
# @flag --trends                              Show pass-rate trends over time (use with --leaderboard).
# @option --trend-days <INTEGER>              Number of days to include in trend analysis (default: 90).
# @flag --adversarial                         Inject adversarial framing into behavioral eval prompts (idea ♯190 Phase 2).
# @flag --no-lessons                          Disable lesson auto-inclusion during eval runs (for no-context baselines).
# @flag --help                                Show this message and exit.
# @arg eval_names_or_result_files*

command eval "$(argc --argc-eval "$0" "$@")"