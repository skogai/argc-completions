#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -a --aliases         alias names
# @flag -d --domain          DNS domain name
# @flag -f                   DNS host name or FQDN
# @flag --fqdn               DNS host name or FQDN
# @flag --long               DNS host name or FQDN
# @option -F --file          set host name or NIS domain name from FILE
# @flag -i --ip-addresses    addresses for the host name
# @flag -s --short           short host name
# @flag -y                   NIS/YP domain name
# @flag --yp                 NIS/YP domain name
# @flag --nis                NIS/YP domain name
# @flag -? --help            give this help list
# @flag --usage              give a short usage message
# @flag -V --version         print program version

command eval "$(argc --argc-eval "$0" "$@")"