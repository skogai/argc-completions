#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -c --container     show container uptime
# @flag -h --no-header     do not print header
# @flag -u --no-current    ignore current process username
# @flag -s --short         short format
# @flag -t --terminal      show terminals
# @flag -f --from          toggle remote hostname field (default: off)
# @flag -o --old-style     old style output
# @flag -i --ip-addr       display IP address instead of hostname (if possible)
# @flag -p --pids          show the PID(s) of processes in WHAT
# @flag --help             display this help and exit
# @flag -V --version       output version information and exit
# @arg user

command eval "$(argc --argc-eval "$0" "$@")"